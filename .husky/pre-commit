#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "Running pre-commit checks..."

# Run lint-staged
pnpm lint-staged

# Run type check on critical packages
echo "Running type checks..."
pnpm --filter @rodistaa/app-shared typecheck || exit 1

echo "✅ Pre-commit checks passed!"

